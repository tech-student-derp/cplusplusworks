#include <iostream>
#include <chrono>
#include <thread>
#include <string>

int main() {
    std::string anim[] = {
        ">_<",
        "O_o",
        "O_O",
        "o_O",
        "o_o"
    }
    int total = sizeof(anim) / sizeof(anim[0]);
    for (int i = 0; i <= 20; i++) {
        std::cout << "\r" <<
    }
}
